# UI Usage
## FACT

## Kibana
Once the first ingestion has been completed, head over to Kibana and select `Menu >> Discover`.

You may be prompted to create an index pattern. Do that and enter the name. Make sure that it matches the other side. The ts_field is optional. 

Once done, head back to the `Discover` pane and the data should automatically be populated